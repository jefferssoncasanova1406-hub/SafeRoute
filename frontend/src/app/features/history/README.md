`history` queda en espera.

Hoy el backend persiste rutas calculadas, pero no expone un endpoint para consultarlas desde el frontend.

Mantener esta carpeta como backlog evita confundir una feature futura con una disponible en la API actual.
